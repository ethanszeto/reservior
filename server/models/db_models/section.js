// need to have a way to get section's associated memory data
// should show up as [Memory: [Section]] in other person's memory feed if mentioned
// group together if multiple sections in shared memories
